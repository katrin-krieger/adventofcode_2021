var commandStrings = `forward 10
                     up 2
                     forward 3
                     down 5`;

describe("Let's dive", () => {
    test("computes correct position", () => {
        expect(true).toBe(true);
    });

    test("creates correct array with command/distance pairs", () => {
        expect;
    });
});
